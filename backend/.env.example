# ==========================================
# Backend Environment Variables
# ==========================================

# Server Configuration
PORT=9000
NODE_ENV=development

# Google Gemini API Configuration
# OPTIONAL: Set this if you want to provide a default API key for all users
# If not set, users must provide their own API key through the frontend
# Get your FREE API key from: https://makersuite.google.com/app/apikey
# No credit card required!
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# ==========================================
# Optional: Google Cloud Configuration
# (Only needed if using server-side voice features)
# ==========================================

# Google Cloud Project ID (optional)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Google Cloud Credentials Path (optional)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ==========================================
# Notes:
# ==========================================
# 1. Copy this file to .env and fill in your values
# 2. Never commit .env to version control
# 3. Gemini API is 100% FREE with generous limits
# 4. Voice features use browser APIs by default (no server-side setup needed)
# 5. NEW: Users can now provide their own API keys through the frontend!
#    - If GEMINI_API_KEY is not set, users MUST provide their own key
#    - If GEMINI_API_KEY is set, it will be used as a fallback
#    - User-provided keys are sent via X-Gemini-API-Key header
